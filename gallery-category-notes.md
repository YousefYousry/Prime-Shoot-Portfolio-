# Gallery category review

- `12.png` shows a styled Weekend Club duo on a studio set. It is a polished campaign frame rather than a behind-the-scenes image.
- `7.png` shows a styled Weekend Club portrait on a studio backdrop. It is also a finished model shot rather than a behind-the-scenes image.
- The current gallery asset set does not contain an explicitly identifiable behind-the-scenes capture from the reviewed files. A third category can be structured now, but accurate assignment requires either a user-supplied behind-the-scenes image or approval to use polished campaign frames as process/archive imagery.
